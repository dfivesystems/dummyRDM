RDMDevice:
    Add More Pids
    Enable loading from template
    Add Custom PIDS
    Messaging

ArtNet:
    handlers:
        add opcodes as appropriate
        Write DMX data to port buffer
        Check TODdata works with greater than 200 devices
    packets:
        Add remaining opcodes

RDM:
    Add many more PIDS

RDMNet:
    Implement broker heartbeat check
    Implement RPT handlers
    Implement Broker handlers

LLRP:
    Make llrp request handler class based

WebEngine:
    Management handlers
    Add/remove device
    Save device as JSON
    
Documentation:
    Type hinting of class members and function arguments
    Keep docstrings up to date
    More detail in docstrings?
